//js code to check whether the given text is a number or not
function text(a) {
    if (isNaN(a) == true) {
        return 'Not a valid number';
    } else if (isNaN(a) == false) {
        return 'A valid number';
    }
}